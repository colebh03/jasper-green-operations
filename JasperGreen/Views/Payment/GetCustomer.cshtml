@model CustomerFilterViewModel

@{
    ViewData["Title"] = "Select Customer";
}

<div class="mx-auto" style="max-width: 900px;">
    <h2 class="mt-3 mb-3">Select Customer</h2>

    <div class="card-box shadow-sm p-3 mx-auto" style="max-width: 900px;">
        <form asp-action="GetCustomer" method="post">
            
            <div class="row mb-2">
                <label asp-for="Cust_ID" class="col-md-3 col-form-label">Customer</label>

                <div class="col-md-7">
                    <select asp-for="Cust_ID"
                            asp-items="@(new SelectList(Model.Customers, "Cust_ID", "Cust_Name"))"
                            class="form-select">
                        <option value="">Select a customer...</option>
                    </select>

                    <span asp-validation-for="Cust_ID" class="text-danger"></span>
                </div>
            </div>

            <div class="mt-3">
                <button type="submit" class="btn btn-green">Select</button>
                <a asp-action="List" class="btn btn-outline-secondary">Cancel</a>
            </div>

        </form>
    </div>
</div>